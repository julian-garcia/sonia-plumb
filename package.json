{
  "name": "sonia-plumb",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run monitor-wp & tsc && vite build -w",
    "build": "npm run copy-wp && npx prettier . --write && tsc && vite build",
    "monitor-wp": "node monitor-wp-files.js",
    "copy-wp": "cp -r wordpress/* dist-local"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.15",
    "postcss": "^8.4.29",
    "prettier": "3.0.3",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
